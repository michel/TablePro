feat: Add comprehensive auto-reconnect for SSH tunnels and database connections

Implements automatic reconnection handling for network interruptions, VPN
disconnections, and database connection failures across all supported
database types.

### SSH Tunnel Auto-Reconnect
- Add health monitoring system that checks tunnel status every 30 seconds
- Detect dead SSH processes automatically (e.g., VPN disconnects)
- Send notifications when tunnels die for coordinated reconnection
- Implement automatic tunnel recreation with 2-second network stabilization delay
- Support all authentication methods (password, private key with passphrase)

### PostgreSQL Auto-Reconnect (NEW)
- Detect PostgreSQL connection failures via error message patterns
- Automatically reconnect and retry failed queries once
- Support both regular execute() and executeParameterized() methods
- Handle connection errors: "server closed", "connection lost", "no connection"

### DatabaseManager SSH Integration
- Listen for SSH tunnel death notifications via NotificationCenter
- Coordinate automatic reconnection attempts with user feedback
- Update connection status during reconnection process
- Fall back to error state with user prompt if reconnection fails
- Preserve session state across reconnection attempts

### Benefits
- Seamless recovery from VPN disconnections without manual intervention
- Automatic handling of temporary network interruptions
- Database server restart tolerance for MySQL, MariaDB, and PostgreSQL
- Improved user experience with transparent reconnection
- Reduced downtime during network issues

### Technical Details
- Uses Swift Concurrency (async/await) for reconnection logic
- Implements retry-once pattern to prevent infinite reconnection loops
- Leverages existing Keychain credential storage for secure reconnection
- Minimal performance impact (30-second health check interval)
- Compatible with existing MySQL/MariaDB auto-reconnect functionality

Fixes issues where users had to manually close and reconnect after VPN
disconnections or network interruptions.
