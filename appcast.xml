<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>TablePro</title>
        <item>
            <title>0.16.0</title>
            <pubDate>Mon, 09 Mar 2026 15:18:10 +0000</pubDate>
            <sparkle:version>29</sparkle:version>
            <sparkle:shortVersionString>0.16.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Fixed</h3>
<ul>
<li>Inspector separator no longer bleeds into toolbar area with default connection color (#228)</li>
<li>Inspector toggle no longer lags due to synchronous UserDefaults writes during animation (#229)</li>
</ul>
<h3>Added</h3>
<ul>
<li>Direct `.tableplugin` bundle installation via file picker, Finder double-click, and drag-and-drop</li>
<li>Plugin capability enforcement — registration now gated on declared capabilities, with validation warnings for mismatches</li>
<li>Plugin dependency declarations — plugins can declare required dependencies via `TableProPlugin.dependencies`, validated at load time</li>
<li>Plugin state change notification (`pluginStateDidChange`) posted when plugins are enabled/disabled</li>
<li>Restart recommendation banner in Settings > Plugins after uninstalling a plugin</li>
<li>Startup commands — run custom SQL after connecting (e.g., SET time_zone) in Connection > Advanced tab</li>
<li>Plugin system architecture — all 8 database drivers (MySQL, PostgreSQL, SQLite, ClickHouse, MSSQL, MongoDB, Redis, Oracle) extracted into `.tableplugin` bundles loaded at runtime</li>
<li>Export format plugins — all 5 export formats (CSV, JSON, SQL, XLSX, MQL) extracted into `.tableplugin` bundles with plugin-provided option views and per-table option columns</li>
<li>Settings > Plugins tab for plugin management — list installed plugins, enable/disable, install from file, uninstall user plugins, view plugin details</li>
<li>Plugin marketplace — browse, search, and install plugins from the GitHub-hosted registry with SHA-256 checksum verification, ETag caching, and offline fallback</li>
<li>TableProPluginKit framework — shared protocols and types for driver and export plugins</li>
<li>ClickHouse database support with query progress tracking, EXPLAIN variants, TLS/HTTPS, server-side cancellation, and Parts view</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Reduce memory: eliminate dedicated ping driver (~30-50 MB per connection), use main driver for health checks</li>
<li>Reduce memory: evict inactive native window-tab row data after 5s, re-fetch on focus</li>
<li>Reduce memory: lazy-load plugin bundles on first use instead of at startup (~20-30 MB saved)</li>
<li>Reduce memory: remove duplicate sourceQuery string from RowBuffer</li>
<li>Reduce memory: InMemoryRowProvider references RowBuffer directly instead of copying rows (~3-10 MB per tab)</li>
<li>Reduce memory: eliminate metadata driver entirely, multiplex all queries on main driver (~30-50 MB per connection)</li>
<li>Reduce memory: lazy AIChatViewModel initialization (deferred until AI panel is first opened)</li>
<li>Reduce memory: remove duplicate connections array from ContentView (use ConnectionStorage.shared directly)</li>
<li>Reduce CPU: consolidate per-editor NSEvent monitors into shared EditorEventRouter singleton (O(n) → O(1) per event)</li>
<li>Fix tab persistence: aggregate tabs from all windows at quit time instead of last-write-wins per-coordinator save</li>
<li>Split DatabaseManager.sessionVersion into fine-grained connectionListVersion and connectionStatusVersion to reduce cascade re-renders</li>
<li>Extract AppState property reads into local lets in view bodies for explicit granular observation tracking</li>
<li>Reorganized project directory structure: Services, Utilities, Models split into domain-specific subdirectories</li>
<li>Database driver code moved from monolithic app binary into independent plugin bundles under `Plugins/`</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/datlechin/TablePro/releases/download/v0.16.0/TablePro-0.16.0-arm64.zip" length="35813112" type="application/octet-stream" sparkle:edSignature="FpHFAAKj3F4woKZew8K9bDhK14JiAHgRdZn7Jx3nwR/kGSghwAtIg9NlFf98Hr20yL6tL5LaRN0koPAEHB4ADA==" sparkle:architectures="arm64"/>
        </item>
        <item>
            <title>0.16.0</title>
            <pubDate>Mon, 09 Mar 2026 15:18:10 +0000</pubDate>
            <sparkle:version>29</sparkle:version>
            <sparkle:shortVersionString>0.16.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<body style="font-family: -apple-system, sans-serif; font-size: 13px; padding: 8px;"><h3>Fixed</h3>
<ul>
<li>Inspector separator no longer bleeds into toolbar area with default connection color (#228)</li>
<li>Inspector toggle no longer lags due to synchronous UserDefaults writes during animation (#229)</li>
</ul>
<h3>Added</h3>
<ul>
<li>Direct `.tableplugin` bundle installation via file picker, Finder double-click, and drag-and-drop</li>
<li>Plugin capability enforcement — registration now gated on declared capabilities, with validation warnings for mismatches</li>
<li>Plugin dependency declarations — plugins can declare required dependencies via `TableProPlugin.dependencies`, validated at load time</li>
<li>Plugin state change notification (`pluginStateDidChange`) posted when plugins are enabled/disabled</li>
<li>Restart recommendation banner in Settings > Plugins after uninstalling a plugin</li>
<li>Startup commands — run custom SQL after connecting (e.g., SET time_zone) in Connection > Advanced tab</li>
<li>Plugin system architecture — all 8 database drivers (MySQL, PostgreSQL, SQLite, ClickHouse, MSSQL, MongoDB, Redis, Oracle) extracted into `.tableplugin` bundles loaded at runtime</li>
<li>Export format plugins — all 5 export formats (CSV, JSON, SQL, XLSX, MQL) extracted into `.tableplugin` bundles with plugin-provided option views and per-table option columns</li>
<li>Settings > Plugins tab for plugin management — list installed plugins, enable/disable, install from file, uninstall user plugins, view plugin details</li>
<li>Plugin marketplace — browse, search, and install plugins from the GitHub-hosted registry with SHA-256 checksum verification, ETag caching, and offline fallback</li>
<li>TableProPluginKit framework — shared protocols and types for driver and export plugins</li>
<li>ClickHouse database support with query progress tracking, EXPLAIN variants, TLS/HTTPS, server-side cancellation, and Parts view</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Reduce memory: eliminate dedicated ping driver (~30-50 MB per connection), use main driver for health checks</li>
<li>Reduce memory: evict inactive native window-tab row data after 5s, re-fetch on focus</li>
<li>Reduce memory: lazy-load plugin bundles on first use instead of at startup (~20-30 MB saved)</li>
<li>Reduce memory: remove duplicate sourceQuery string from RowBuffer</li>
<li>Reduce memory: InMemoryRowProvider references RowBuffer directly instead of copying rows (~3-10 MB per tab)</li>
<li>Reduce memory: eliminate metadata driver entirely, multiplex all queries on main driver (~30-50 MB per connection)</li>
<li>Reduce memory: lazy AIChatViewModel initialization (deferred until AI panel is first opened)</li>
<li>Reduce memory: remove duplicate connections array from ContentView (use ConnectionStorage.shared directly)</li>
<li>Reduce CPU: consolidate per-editor NSEvent monitors into shared EditorEventRouter singleton (O(n) → O(1) per event)</li>
<li>Fix tab persistence: aggregate tabs from all windows at quit time instead of last-write-wins per-coordinator save</li>
<li>Split DatabaseManager.sessionVersion into fine-grained connectionListVersion and connectionStatusVersion to reduce cascade re-renders</li>
<li>Extract AppState property reads into local lets in view bodies for explicit granular observation tracking</li>
<li>Reorganized project directory structure: Services, Utilities, Models split into domain-specific subdirectories</li>
<li>Database driver code moved from monolithic app binary into independent plugin bundles under `Plugins/`</li>
</ul></body>]]></description>
            <enclosure url="https://github.com/datlechin/TablePro/releases/download/v0.16.0/TablePro-0.16.0-x86_64.zip" length="33495913" type="application/octet-stream" sparkle:edSignature="jZ3uQrGF3cgyAQP3obM8452x65361Aq8RM07DF7/PH4jDs5Z9KPdepU2befAzN97AnNGn8W92FRfFEouLAGhAA==" sparkle:architectures="x86_64"/>
        </item>
    </channel>
</rss>
